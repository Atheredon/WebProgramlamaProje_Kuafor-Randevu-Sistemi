
<!DOCTYPE html>

<html lang="en" data-bs-theme="auto">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"]</title>
    <link href="/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark py-1">
            <div class="container-fluid">
                <!-- Home Link -->
                <a class="navbar-brand" href="/">Salon Kuaför</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarCollapse">

                    <!-- Left-aligned links -->
                    <ul class="navbar-nav me-auto mb-2 mb-md-0">
                        <li class="nav-item">
                            <a class="nav-link" href="/Home/Services">Servislerimiz</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/AI/UploadImage">AI ile Kendinizde Görün</a>
                        </li>
                        @if (ViewData["UserRole"]?.ToString() == "Customer")
                        {
                            <li class="nav-item">
                                <a class="nav-link" href="/Customer/BookAppointment">Randevu Al</a>
                            </li>
                        }
                        @if (ViewData["UserRole"]?.ToString() == "Admin")
                        {
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle btn" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                    Admin Paneli
                                </a>
                                <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
                                    <li><a class="dropdown-item" href="/Admin/Services">Hizmetler</a></li>
                                    <li><a class="dropdown-item" href="/Admin/Staff">Çalışanlar</a></li>
                                    <li><a class="dropdown-item" href="/Admin/Customers">Müşteriler</a></li>
                                    <li><a class="dropdown-item" href="/Admin/Analytics">Analytics</a></li>
                                </ul>
                            </li>
                        }
                        @if (ViewData["UserRole"]?.ToString() == "Staff")
                        {
                            <li class="nav-item">
                                <a class="nav-link" href="/Staff/ManageMyAppointments">Randevularımı işlet</a>
                            </li>
                        }
                    </ul>

                    <!-- Right-aligned links -->
                    <ul class="navbar-nav">
                        @if (ViewData["UserId"] != null)
                        {
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle btn btn-primary text-white" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                    @ViewData["UserName"]
                                </a>
                                <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
                                    <li><a class="dropdown-item" href="/Account/ViewDetails">Hesap Detayları</a></li>
                                    <li><a class="dropdown-item" href="/Customer/MyAppointments">Randevularım</a></li>
                                    <li><hr class="dropdown-divider"></li>
                                    <li><a class="dropdown-item text-danger" href="/Account/Logout">Çıkış Yap</a></li>
                                </ul>
                            </li>
                        }
                        else
                        {
                            <li class="nav-item">
                                <a class="nav-link btn btn-primary text-white me-2" href="/Account/Login">Giriş Yap</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link btn btn-primary text-white me-2" href="/Account/SignUp">Kaydol</a>
                            </li>
                        }
                    </ul>
                </div>
            </div>
        </nav>
    </header>

    <div style="height: 3rem;"></div>

    <!-- Main content -->
    <main class="container mt-5">
        @RenderBody()
    </main>

    <!-- Footer -->
    <footer class="container text-center mt-5">
        <p>Salon Kuaför</p>
    </footer>
    <script src="/js/bootstrap.bundle.min.js"></script>
</body>
</html>
