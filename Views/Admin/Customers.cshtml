<h1>Müşteriler</h1>

<table class="table table-striped">
    <thead>
        <tr>
            <th>İsim</th>
            <th>Email</th>
            <th>Aksiyonlar</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var customer in Model)
        {
            <tr>
                <td>@customer.Name @customer.Surname</td>
                <td>@customer.Email</td>
                <td>
                    <a class="btn btn-primary btn-sm" href="/Admin/CustomerDetails?customerId=@customer.Id">Detayları Görüntüle</a>
                </td>
            </tr>
        }
    </tbody>
</table>
